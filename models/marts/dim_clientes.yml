models:
  - name: dim_clientes
    description: Essa é a dimensão que informa os clientes da Adventure
    columns:
      - name: pk_cliente
        description: Essa é a chave primaria da dimensão clientes
        tests:
          - unique
          - not_null